# Vercel Deployment Secrets
# Get these from the Vercel dashboard under Project Settings > General
VERCEL_TOKEN=
VERCEL_ORG_ID=
VERCEL_PROJECT_ID=

# Auth.js (NextAuth v5) -- GitHub OAuth
# 1. Create a GitHub OAuth App at https://github.com/settings/developers
# 2. Set the callback URL to your prod url
#    (and http://localhost:3000/api/auth/callback/github for local dev)
# 3. Generate AUTH_SECRET with: npx auth secret
AUTH_SECRET=
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=

# Neon Postgres
# Get this from the Vercel dashboard (Storage > your Neon database > .env tab)
# or from the Neon console at https://console.neon.tech
# Use the pooled connection string (the one with "-pooler" in the hostname)
DATABASE_URL=
